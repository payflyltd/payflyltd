import { getAllJobs, getJobById, createJob, updateJob, deleteJob } from "../services/db.service"

// Re-export the functions from the database service
export { getAllJobs, getJobById, createJob, updateJob, deleteJob }
